import React from 'react';

const ExampleComponent = () => {
    return (
        <div>
            <p>This is an example component rendered by React SSR dupa.</p>
        </div>
    );
};

export default ExampleComponent;